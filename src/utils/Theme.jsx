import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    fontSize: 16,
    h1: {
      fontSize: "1.8rem",
    },
    body1: {
      fontSize: "0.85rem",
    },
  },
});

export default theme;
